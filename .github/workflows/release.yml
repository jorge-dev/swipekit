name: release

# Push a version tag to cut a release:
#   git tag v0.1.0 && git push origin v0.1.0
# The gate runs first; then a GitHub Release is created with the notes for that
# version taken straight from CHANGELOG.md.

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # create the GitHub Release
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          package-manager-cache: false  # release runs rarely; no cache to poison

      - run: npm ci --ignore-scripts

      # Never publish a release off a red tree.
      - run: npm run check

      - name: Extract this version's changelog section
        id: notes
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v v="$VERSION" '
            $0 ~ "^## \\[" v "\\]"      { grab=1; next }
            grab && /^## \[/            { exit }
            grab && /^\[[^]]+\]: http/  { exit }
            grab                        { print }
          ' CHANGELOG.md > release-notes.md
          # trim leading/trailing blank lines
          sed -i -e '/./,$!d' release-notes.md
          if [ ! -s release-notes.md ]; then
            echo "No '## [$VERSION]' section found in CHANGELOG.md" >&2
            exit 1
          fi
          echo "Release notes:"
          cat release-notes.md

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes-file release-notes.md
