# Codex-specific metadata. Other agents ignore this file; the portable skill is SKILL.md.
interface:
  display_name: "swipekit"
  short_description: "Research which TikTok slideshow formats are winning in a niche, with numbers a reader could check."

policy:
  # Let Codex pick this up on its own when a prompt matches the description in SKILL.md.
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "swipekit"
      description: "Local TikTok slideshow research server (swipekit-mcp). Register with: codex mcp add swipekit -- swipekit-mcp"
      transport: "stdio"
